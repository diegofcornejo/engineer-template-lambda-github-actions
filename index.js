exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Engineer AWS Lambda CI/CD with github actions'),
    };
    return response;
};